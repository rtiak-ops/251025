# 🚀 AWS EC2 デプロイ完全ガイド (初心者向け)

このガイドでは、AWSの基本を学びながら、あなたのアプリを世界中に公開する手順を詳しく解説します。
まずは「1台のサーバーですべて動かす」という基本構成（ステップ1）をマスターしましょう。

---

## 📋 準備するもの
- AWS アカウント
- クレジットカード（無料枠の範囲内なら課金はほぼされませんが、登録に必要です）
- やる気！

---

## 🛠️ ステップ 1: AWS コンソールで「サーバー」を借りる

まずは自分専用のパソコン（サーバー）をクラウド上に作成します。

1. **EC2 ダッシュボードへ移動**
   - AWSコンソールの検索窓に「EC2」と入力し、クリックします。
2. **インスタンスを起動**
   - 「インスタンスを起動」というオレンジ色のボタンをクリックします。
3. **名前とタグ**
   - 名前: `my-todo-app` など好きな名前を付けます。
4. **OS (Amazon マシンイメージ)**
   - **Amazon Linux 2023 AMI** を選択します（無料利用枠の対象）。
5. **インスタンスタイプ**
   - **t3.micro**（または `t2.micro`）を選択します。
6. **キーペア (ログインに必要)**
   - 「新しいキーペアの作成」をクリック。
   - キーペア名: `todo-key`
   - キーペアのタイプ: `RSA`
   - プライベートキーファイル形式: `.pem`
   - **[重要]** 作成するとファイルがダウンロードされます。**二度とダウンロードできないので、大切に保管してください。**
7. **ネットワーク設定 (ファイアウォール)**
   - 「SSH トラフィックを許可」にチェック（自分のIPのみにすると安全）。
   - **「インターネットからの HTTP トラフィックを許可」にチェック。**
   - **「インターネットからの HTTPS トラフィックを許可」にチェック。**
8. **起動**
   - 右下の「インスタンスを起動」をクリックします。

---

## � ステップ 2: サーバーにログインする (SSH)

Windows の PowerShell やコマンドプロンプトを使って、作成したサーバーの中に入ります。

1. **パブリック IP アドレスを確認**
   - AWSコンソールのインスタンス一覧から、作成したインスタンスをクリックし「パブリック IPv4 アドレス」をコピーします。
2. **ログインコマンドの実行**
   - `.pem` ファイルがあるフォルダで Shift + 右クリック → 「PowerShell を開く」。
   - 以下のコマンドを入力します（`your-ip` は自分のアドレスに、`todo-key.pem` は自分のファイルパスに書き換えてください）。
   ```powershell
   ssh -i "todo-key.pem" ec2-user@your-ip
   ```
   - `Are you sure you want to continue?` と聞かれたら `yes` と入力。

---

## 📦 ステップ 3: 必要なソフトのインストール

ログインできたら、以下のコマンドを順番にコピー＆ペーストして実行してください。

```bash
# 1. OSを最新にする
sudo dnf update -y

# 2. Docker と Git を入れる
sudo dnf install -y docker git
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker ec2-user

# 3. Docker Compose を入れる
# フォルダを確実に作成
mkdir -p ~/.docker/cli-plugins

# 4. 最新の Buildx を再ダウンロード (URLを最新版に修正しています)
curl -SL https://github.com/docker/buildx/releases/download/v0.19.3/buildx-v0.19.3.linux-amd64 -o ~/.docker/cli-plugins/docker-buildx

# 5. 実行権限を確実に付与
chmod +x ~/.docker/cli-plugins/docker-buildx

# 6. 正しく認識されたかバージョンを確認 (数字が表示されればOK)
docker buildx version

# 7. 設定反映のため一度ログアウト
exit
```

**※ コマンドが終わったら、もう一度 SSH でログインし直してください。**

---

## 🚀 ステップ 4: アプリのデプロイ

いよいよアプリを動かします。

1. **コードのダウンロード**
   ```bash
   git clone https://github.com/rtiak-ops/251025
   cd 251025
   ```
2. **環境設定 (.env)**
   - サンプルの `.env` をコピーして作成します。
   ```bash
   cp .env.example .env
   ```
   - 内容を編集したい場合は `nano .env` で編集できます（保存は Ctrl+O, 終了は Ctrl+X）。
3. **アプリの起動**
   ```bash
   docker compose up -d --build
   ```

✅ **確認**: ブラウザで `http://あなたのパブリックIP` を開き、アプリが表示されれば成功です！

---

## 🔄 運用とメンテナンス

### コードを更新したとき
```bash
git pull origin main
docker compose up -d --build
```

### 動きがおかしいとき（ログ確認）
```bash
docker compose logs -f
```

---

## 💰 ステップ 5: 課金されないために (重要)

使い終わった、または一度リセットしたい場合は、必ず以下のどちらかを行ってください。

1. **インスタンスを停止 (Stop)**: サーバーが止まります。コンピューティング料金はかかりませんが、ディスク容量（EBS）の料金がわずかにかかります。
2. **インスタンスを終了 (Terminate)**: サーバー自体を消去します。**すべての料金が止まります。** 次に使うときはまたステップ1からになります。

次は、データを安全に分ける「RDS（データベース）編」に進みましょう！
