# 🚀 AWS EC2 詳細デプロイガイド (最安構成)

このガイドでは、AWS の無料枠を活用して、ToDo アプリを 1 から公開するまでの全手順を解説します。

---

## 📅 全体の流れ
1. **AWS コンソール操作**: サーバー（EC2）を借りる
2. **接続**: 自分の PC からサーバーにログインする
3. **準備**: サーバーに Docker などの必要なソフトを入れる
4. **デプロイ**: アプリを動かす

---

## 🛠️ ステップ 1: AWS でサーバーを借りる (EC2)

1. **EC2 ダッシュボードへ**:
   - AWS コンソールで「EC2」を検索し、[インスタンスを起動] ボタンをクリックします。

2. **基本設定**:
   - **名前**: `todo-app-server` (任意)
   - **OS (AMI)**: `Amazon Linux 2023` (デフォルトでOK。Free Tier eligible と書いてあるもの)
   - **インスタンスタイプ**: `t3.micro` (無料枠対象)

3. **キーペア (ログイン用鍵)**:
   - [新しいキーペアの作成] をクリック。
   - 名前: `todo-key`
   - 形式: `.pem` (Windows で標準的な SSH を使う場合)
   - **大切**: ダウンロードしたファイルは二度と再取得できません。安全な場所に保存してください。

4. **ネットワーク設定 (重要)**:
   - [編集] ボタンをクリック。
   - **パブリックIPの自動割り当て**: `有効` になっていることを確認。
   - **セキュリティグループ (防火壁)**:
     - `SSH`: ポート 22, ソース「自分のIP」
     - `HTTP`: ポート 80, ソース「Anywhere (0.0.0.0/0)」
     - `HTTP (Vite開発用テスト目的など)` : ポート 5173 を開けておくとデバッグに便利ですが、今回は 80 番のみでOKです。

5. **起動**:
   - 右下の [インスタンスを起動] をクリック。

---

## 🛠️ ステップ 2: サーバーにログインする (SSH)

1. **IPアドレスを確認**:
   - インスタンス一覧から、作成したものを選び「パブリック IPv4 アドレス」をコピーします (例: `54.12.34.56`)。

2. **ターミナル (PowerShellなど) を開く**:
   - キーペアを保存したフォルダに移動して、以下のコマンドを実行します。
   ```bash
   # 鍵に適切な権限を付与 (Windowsは不要な場合が多いですが、エラーが出る場合はプロパティから設定)
   # 接続コマンド (IPの部分は自分のものに書き換え)
   ssh -i "todo-key.pem" ec2-user@54.12.34.56
   ```
   - 「Are you sure you want to continue connecting?」には `yes` と答えます。

---

## 🛠️ ステップ 3: サーバーのセットアップ

ログインに成功したら、以下のコマンドを順番にコピペして実行してください。

1. **Docker のインストール**:
   ```bash
   sudo dnf update -y
   sudo dnf install -y docker
   sudo systemctl start docker
   sudo systemctl enable docker
   sudo usermod -aG docker ec2-user
   ```

2. **Docker Compose のインストール**:
   ```bash
   sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
   sudo chmod +x /usr/local/bin/docker-compose
   ```

3. **一度ログアウト**:
   - グループ設定を反映させるため、一度接続を切ります。
   ```bash
   exit
   ```
   - その後、再度 `ssh -i ...` でログインしてください。

---

## 🛠️ ステップ 4: アプリの起動

1. **ソースコードを取得**:
   ```bash
   git clone https://github.com/rtiak-ops/251025
   cd 251025
   ```

2. **環境変数の設定 (任意)**:
   - OpenAI の API キーを使いたい場合は、`.env` ファイルを作ります。
   ```bash
   echo "OPENAI_API_KEY=sk-xxxx" > .env
   ```

3. **魔法のコマンドで起動**:
   ```bash
   docker-compose up -d --build
   ```

---

## ✅ 動作確認
1. ブラウザで `http://[あなたのパブリックIP]` を開きます。
2. アプリが表示されれば成功です！ 🎉

---

## 💡 トラブルシューティング
- **画面が表示されない**: セキュリティグループで 80 番ポートが開いているか再確認してください。
---

## 🧹 お金が発生しないようにするための削除手順 (後片付け)

「使い終わった」「一度リセットしたい」という場合に、料金が発生しないようリソースを完全に削除する手順です。

1. **EC2 インスタンスの終了**:
   - EC2 ダッシュボードの [インスタンス] 一覧から、対象のサーバーを選択します。
   - [インスタンスの状態] > **[インスタンスを終了]** をクリックします。
   - ※「停止」ではなく「終了」を選ぶのがポイントです。「終了」にすると、数分後にサーバーが消滅し、課金が止まります。

2. **ストレージ (EBS) の確認**:
   - 通常、インスタンスを「終了」すると、セットで作られたディスク（EBS）も自動で消えます。
   - 念のため、左メニューの [ボリューム] を開き、状態が `available`（使用されていない）になっているものがあれば、選択して [アクション] > [ボリュームの削除] を行ってください。

3. **弾力性のある IP (Elastic IP) の解放** (もし取得していた場合):
   - 左メニューの [Elastic IP] を確認し、もし割り当てられているアドレスがあれば、[アクション] > **[Elastic IP アドレスの解放]** を行ってください。
   - ※使用していない Elastic IP は維持費がかかるため、必ず解放しましょう。

4. **キーペアとセキュリティグループ**:
   - これらは置いておくだけなら **無料** です。次回また使う可能性があるなら残しておいて構いません。

---
