# 🐘 AWS RDS 移行ガイド (データベースの分離)

EC2上でのデプロイが完了したら、次はデータベースをコンテナから切り離して、AWSの専用DBサービスである **RDS (Relational Database Service)** に移行しましょう。これにより、データの安全性とシステムの拡張性が大幅に向上します。

---

## 🛠️ ステップ 1: AWS RDS インスタンスの作成

1.  **RDS ダッシュボードへ移動**
    *   AWSコンソールの検索窓に「RDS」と入力し、クリックします。
2.  **データベースの作成**
    *   「データベースの作成」ボタンをクリックします。
3.  **エンジンの選択**
    *   **PostgreSQL** を選択します。
    *   エンジンバージョン: 現在のプロジェクトに合わせて **17.x** を推奨します。
4.  **テンプレート**
    *   **無料利用枠** を選択します（学習用・開発用に最適です）。
5.  **設定**
    *   DBインスタンス識別子: `database-app`
    *   マスターユーザー名: `postgresMaster`
    *   マスターパスワード: **[重要] 強力なパスワードを設定し、必ず控えておいてください。**
6.  **接続**
    *   **VPC**: EC2インスタンスと同じVPCを選択します。
    *   **パブリックアクセス**: `なし`（セキュリティを考慮し、EC2経由でのみアクセスさせます）。
    *   **VPC セキュリティグループ**: 「新規作成」を選択し、名前に `rds-sg` と入力します。
7.  **追加の設定**（一番下の折りたたまれているメニュー）
    *   最初のデータベース名: `todo_production_db`
8.  **作成**
    *   右下の「データベースの作成」をクリックします。作成には5〜10分かかります。

---

## 🔒 ステップ 2: セキュリティグループの設定 (通信許可)

EC2からRDSにアクセスできるように「鍵」を開けます。

1.  **RDSの詳細画面**
    *   作成した `todo-db-production` をクリックします。
2.  **セキュリティグループの編集**
    *   「接続とセキュリティ」タブにある「VPC セキュリティグループ (rds-sg)」をクリックします。
3.  **インバウンドルールの編集**
    *   「インバウンドルールを編集」をクリックし、新しいルールを追加します。
    *   **タイプ**: `PostgreSQL (5432)`
    *   **ソース**: `カスタム` を選択し、横の検索窓で **EC2が使っているセキュリティグループのID** (例: `sg-xxxxxxxx`) を検索・選択します。
    *   「ルールを保存」をクリックします。

---

## 🚚 ステップ 3: データの移行 (バックアップ & 復元)

EC2上のターミナルで実行します。コンテナ内のデータをRDSへコピーします。

1.  **RDSのエンドポイントを確認**
    *   RDSの詳細画面で「エンドポイント」をコピーします（例: `xxx.xxx.ap-northeast-1.rds.amazonaws.com`）。
2.  **コンテナからデータを書き出し (Dump)**
    ```bash
    docker compose exec db pg_dump -U todo_admin_user todo_production_db > backup.sql
    ```
3.  **RDSへデータを流し込み (Restore)**
    *   `DB_HOST` にコピーしたエンドポイントを入れます。
    ```bash
    docker compose exec db psql -h DB_HOST -U todo_admin_user -d todo_production_db -f backup.sql
    ```
    *   パスワードを聞かれるので、ステップ1で設定したものを入力します。

---

## ⚙️ ステップ 4: アプリの設定変更 (`.env`)

1.  **.env ファイルを編集**
    ```bash
    nano .env
    ```
2.  **DATABASE_URL を更新**
    *   `db:5432` だった部分を RDSのエンドポイントに変更します。
    ```bash
    # 変更後
    DATABASE_URL=postgresql+asyncpg://todo_admin_user:パスワード@RDSのエンドポイント:5432/todo_production_db
    ```
    *   保存 (Ctrl+O, Enter) して終了 (Ctrl+X) します。

---

## 🧹 ステップ 5: 仕上げと不要なコンテナの停止

1.  **アプリの再起動**
    ```bash
    docker compose up -d
    ```
    *   ブラウザで `http://あなたのIP` を開き、データが表示されるか確認します。
2.  **docker-compose.yml の整理**
    *   RDSに移行したので、EC2内の `db` サービスは不要になります。
    *   `docker-compose.yml` を開き、以下を修正します。
        *   `services:` 内の `db:` セクションをすべて削除（またはコメントアウト）。
        *   `backend:` 内の `depends_on:` から `db:` を削除。
3.  **最終反映**
    ```bash
    docker compose up -d --remove-orphans
    ```
    *   これで、EC2上ではアプリ本体のみが動き、データはRDSに保存される理想的な構成になります！

---

✅ **お疲れ様でした！これでクラウドらしい構成が整いました。**
