# 🌐 S3 + CloudFront 配信ガイド (フロントエンドの最適化)

フロントエンド（React/Vite）を EC2 から切り離し、**S3** と **CloudFront** で配信するように変更します。これにより、表示速度の向上、EC2負荷の軽減、そしてコスト削減が期待できます。

---

## 🏗️ 全体像
1.  **S3**: 静的なファイルを保存する場所（倉庫）。
2.  **CloudFront**: 世界中のサーバーから最速でファイルを届ける仕組み（配送網）。
3.  **EC2**: バックエンド API 専用サーバーとして動作。

---

## 🛠️ ステップ 1: フロントエンドのビルド

S3にアップロードするためのファイルを作成します。

1.  **接続先の設定変更**:
    *   フロントエンドの `api.ts` は `VITE_API_BASE_URL` を参照しています。
    *   ビルド時に EC2 の IP（またはドメイン）を指定する必要があります。
2.  **ビルドの実行 (ローカルまたはEC2で)**:
    ```bash
    cd frontend
    # EC2のIPが 1.2.3.4 の場合
    VITE_API_BASE_URL=http://1.2.3.4 npm run build
    ```
    *   `dist` フォルダが作成され、その中に `index.html` や `assets/` が入ります。

---

## 📦 ステップ 2: S3 バケットの作成とアップロード

1.  **S3 バケットを作成**:
    *   名前: `my-todo-frontend-bucket` (世界で唯一の名前)
    *   リージョン: `ap-northeast-1` (東京)
    *   **「パブリックアクセスをすべてブロック」はチェックしたまま**でOK。
2.  **ファイルをアップロード**:
    *   `dist` フォルダの中身（フォルダごとではなく、**中身すべて**）をバケットのルートへドラッグ＆ドロップしてアップロードします。

---

## 🚀 ステップ 3: CloudFront の設定

1.  **ディストリビューションを作成**:
    *   **オリジンドメイン**: 作成した S3 バケットを選択。
    *   **オリジンアクセス**: `Origin access control settings (recommended)` を選択。
    *   **コントロール設定を作成**: デフォルト設定で「作成」。
    *   **ビューワー`: `Redirect HTTP to HTTPS` を推奨。
    *   **デフォルトルートオブジェクト**: `index.html` と入力。
2.  **S3 バケットポリシーの更新**:
    *   作成完了後、画面上部に「S3 バケットポリシーを更新する必要があります」と表示されるので、「ポリシーをコピー」をクリック。
    *   S3 の設定画面に戻り、「アクセス許可」→「バケットポリシー」に貼り付けて保存します。

---

## ⚙️ ステップ 4: バックエンド (FastAPI) の CORS 設定

フロントエンドの URL が変わるため、バックエンド側で許可を与える必要があります。

1.  **EC2 上の `.env` を編集**:
    ```bash
    # CloudFront のドメイン (例: d1234567.cloudfront.net) を追加
    CORS_ORIGINS=https://d1234567.cloudfront.net,http://localhost:5173
    ```
2.  **バックエンドを再起動**:
    ```bash
    docker compose up -d backend
    ```

---

## 🧹 ステップ 5: EC2 側の整理

1.  **docker-compose.yml の修正**:
    *   もう EC2 でフロントエンドを動かす必要はありません。
    *   `frontend` サービスを削除（またはコメントアウト）します。
    *   ポート 80/443 の開放も不要になります。
2.  **コンテナの停止**:
    ```bash
    docker compose up -d --remove-orphans
    ```

---

## 💡 注意点 (Single Page Application 対応)
React などの SPA を CloudFront で配信する場合、画面をリロードすると 403 エラーになることがあります。これを防ぐ設定が必要です：

1.  CloudFront の設定で「エラーレスポンス」タブへ移動。
2.  「カスタムエラーレスポンスを作成」をクリック。
3.  **HTTP エラーコード**: `403` (または 404)
4.  **レスポンスページパス**: `/index.html`
5.  **HTTP レスポンスコード**: `200`
これで、どの URL でリロードしても React が正しく起動します。

---

✅ **これで、プロレベルのモダンな配信構成が完成です！**
