# backend/pytest.ini
[pytest]
# 非同期テストを自動検出し、STRICTモードのエラーを回避
asyncio_mode = auto
# 非同期ループのスコープをセッション単位に設定（セッションスコープの非同期fixtureを許可するため）
asyncio_default_fixture_loop_scope = session
# テスト対象ディレクトリを指定
testpaths = tests
# インポートパスを通す
pythonpath = .