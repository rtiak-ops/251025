# backend/pytest.ini
[pytest]
# 非同期テストを自動検出し、STRICTモードのエラーを回避
asyncio_mode = auto
# 非同期ループのスコープを関数単位に設定（最新版の推奨設定）
asyncio_default_fixture_loop_scope = function
# テスト対象ディレクトリを指定
testpaths = tests
# インポートパスを通す
pythonpath = .